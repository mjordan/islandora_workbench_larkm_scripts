[tool.setuptools.packages.find]
namespaces = false

[project]
name = "Islandora Workbench larkm scripts"
version = "1.0.0"
description = "A lightweight ARK manager"
requires-python = ">=3.10"
dependencies = [
    "requests",
    "ruamel.yaml",
    "persist-queue"
]
